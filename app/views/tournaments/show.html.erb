
<div class="container-fluid  body-container">
  <h1><%= "#{@tournament.name}" %> </h1>
  <ul>
    <% @tournament.teams.each do |t| %>
      <li><%= link_to "#{t.name}", t %></li>
    <% end %>
  </ul>
  <%= link_to "Add Team", all_teams_path, class:"btn btn-primary" %>
</div>
